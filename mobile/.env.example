# Use the public wrapper served by web/, never the private cobalt API on port 9000.
# Replace this example with the development computer's LAN address or a deployed URL.
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
